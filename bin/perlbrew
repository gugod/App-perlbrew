#!perl -l
use strict;
use App::perlbrew;
use Getopt::Long qw(:config pass_through);

my $opt = {};
my $result = GetOptions( 'f|force!' => \$opt->{force}, );

App::perlbrew->run_command($opt, @ARGV );
